# 个人主页每日开发日志系统 — 上下文说明

> 用于维护/交接 `hong2301/hong2301`（GitHub 个人主页仓库）的每日自动日志系统。
> 本文件为本地参考（已 gitignore，不推送到 GitHub）。

---

## 一、系统目标

GitHub 个人主页（github.com/hong2301）的 README 每天自动展示**前一天的开发日志**：
- AI 总结当天全部提交（30-500字，只记录+评价，禁止未来推测/情绪）
- 主题色每天由 AI 从 8 色调色板中选一个，全篇统一（标题/词云/饼图）
- 提交消息词云 + 时间分布环形饼图（12/3/6/9 时钟刻度）
- 历史日志按日期存档到 `logs/YYYY-MM-DD/`

## 二、触发机制（GitHub Actions）

- Workflow：`.github/workflows/daily-log.yml`（唯一，名 "Daily Dev Log"）
- 定时：`cron: "0 16 * * *"` = **每天 UTC 16:00（北京 0 点）**
- 语义：**统计"昨天北京一整天"**（00:00-23:59 北京时间），生成"昨天"的日志
  - 北京 = UTC+8；查询范围 = `committer-date:>=昨天北京0点(UTC)` 且 `<今天北京0点(UTC)`
- 也支持 `workflow_dispatch` 手动触发
- 权限：`contents: write`；自动提交消息带 `[skip ci]`，避免触发自身

## 三、脚本：`.github/scripts/gen_log.py`

流程：
1. **取提交**：GitHub Search Commits API（`q=author:hong2301+committer-date:YYYY-MM-DD`），翻页取全
   - ⚠️ **重要**：`committer-date` 只支持精确日期（YYYY-MM-DD），**不支持范围查询**（`>=`、`<` 会失效）
   - 解决方案：查两天精确日期（UTC start 和 end），再过滤出北京时间昨天全天的提交
2. **AI 总结 + 主题色**：DeepSeek API
   - 模型：`deepseek-chat`（**不要用 `deepseek-v4-flash`，那是推理模型，token 全用在思考上导致 content 为空**）
   - 端点：`https://api.deepseek.com/chat/completions`
   - Token：仓库 Secret `DEEPSEEK_TOKEN`
   - 要求输出严格 JSON：`{"text": "...", "color": "blue"}`
   - **fallback**：JSON 解析失败时用正则提取 `"text"` 字段值（不要直接回显 JSON 原文）
   - 日志规则：30-500字、第一人称、客观记录+当天简评、项目/版本带中文名、**禁止未来推测与情绪**
3. **主题色板**（低饱和易读）：
   ```python
   PALETTE = {blue:#58a6ff, green:#3fb950, purple:#bc8cff, orange:#d29922,
              cyan:#39c5cf, pink:#f778ba, teal:#4ec9b0, yellow:#e3b341}
   ```
4. **词云**：wordcloud + jieba（中文分词），PIL 转透明 RGBA PNG（白底转透明），`630x350`
   - 颜色 = 主题色系（color_func 明度随机）
5. **饼图**：SVG 矢量（透明背景），环形扇区（**注意 path 必须含 `L` 连到内圆**，否则形状错乱），
   12/3/6/9 时钟刻度、中心"总数/次提交"，文字用主题色
   - 尺寸 `420x350`
6. **README 组装**：
   - 标题：`<h1 style="color:{主题色}">YYYY年M月D日日志</h1>`
   - 正文每段前加两个全角空格 `　　`（按 `\n` 分段逐段缩进）
   - 图片引用：**仓库内相对路径 + 版本参数防 CDN 缓存**：
     `<img src="wordcloud.png?v=YYYYMMDD" width="49%"> <img src="pie.svg?v=YYYYMMDD" width="32.7%">`
     （宽度比 3:2 保证显示高度一致）
   - 正文与图片之间一条 `---` 分割线
   - 末尾：`📚 查看历史日志 ./logs/`
7. **存档**：`logs/YYYY-MM-DD/日志.md`（同 README 内容）+ 当日 `wordcloud.png`、`pie.svg`

## 四、图片显示经验（重要踩坑）

- ❌ **base64 嵌入 README 在 GitHub 不渲染**（已确认无效，勿再用）
- ✅ **仓库内相对路径引用**是 GitHub 原生最可靠方式，加 `?v=日期` 防 CDN 缓存
- ⚠️ raw.githubusercontent.com / jsDelivr 在部分网络时通时断；若用户网络访问 GitHub 不稳会显示不出
- 图片文件（wordcloud.png、pie.svg）必须提交在仓库**根目录**

## 五、环境依赖（workflow 内安装）

```yaml
# 拆分成独立步骤（避免 apt-get 卡住影响 pip 安装）
- name: 安装中文字体
  run: sudo apt-get update -qq && sudo apt-get install -y -qq fonts-wqy-microhei
- name: 安装依赖(词云+中文分词)
  run: pip install -r requirements.txt   # wordcloud(含 matplotlib) + jieba
```

- `requirements.txt` 包含：`wordcloud`、`jieba`
- `wordcloud` 依赖 `matplotlib`，会自动安装（numpy、pillow、contourpy 等）
- 脚本读取 `FONT_PATH` 环境变量（本地测试用 Windows 的 `C:/Windows/Fonts/simhei.ttf`，
  workflow 用 `/usr/share/fonts/truetype/wqy/wqy-microhei.ttc`）

## 六、Secrets

- `GH_TOKEN`：自动用 `${{ secrets.GITHUB_TOKEN }}`（无需配置）
- `DEEPSEEK_TOKEN`：DeepSeek API Key（已配置，需充值才有额度）

## 七、本地测试命令

```bash
cd <个人主页仓库>
export GH_TOKEN=$(gh auth token)
export DEEPSEEK_TOKEN="sk-..."
export FONT_PATH="C:/Windows/Fonts/simhei.ttf"
python .github/scripts/gen_log.py
```
生成根目录 `README.md`、`wordcloud.png`、`pie.svg`，存档 `logs/今天昨天/`。

## 八、注意事项

- 每天 0 点自动跑，生成**昨天**日志；提交多时（几百条）不截断，靠 `max_tokens=2000` 保证完整输出
- 若 GitHub API 限流或 DeepSeek 无额度：脚本 fallback（无 AI 文本时用"今天没有提交记录"占位），workflow 仍 success
- 勿手动向该仓库 push 大量无关内容；Actions 的 `[skip ci]` 提交避免递归触发

## 九、踩坑记录（2026-08-18 修复）

### 1. GitHub API committer-date 范围查询失效
- **问题**：`committer-date:>=2026-08-17T16:00:00Z+committer-date:<2026-08-18T16:00:00Z` 范围查询完全不生效，返回所有提交（包括2025年的）
- **根因**：GitHub Search Commits API 的 `committer-date` 只支持精确日期（YYYY-MM-DD），不支持 `>=`、`<`、`>` 等范围操作符
- **解决**：查两天精确日期（UTC start 和 end），再用 Python 过滤出北京时间昨天全天的提交
- **验证**：精确日期查询 `committer-date:2026-08-18` 返回 16 条（正确），范围查询返回 991 条（错误）

### 2. DeepSeek v4-flash 推理模型导致 AI 内容为空
- **问题**：AI 返回空字符串，日志显示"日志生成中..."
- **根因**：`deepseek-v4-flash` 是推理模型，token 全用在 `reasoning_content`（思考过程）上，`content`（最终输出）为空
- **解决**：改用 `deepseek-chat`（普通模型）
- **验证**：`deepseek-chat` 返回 `content: "好"`，`deepseek-v4-flash` 返回 `content: ""` + `reasoning_content: "..."`

### 3. 正则表达式语法错误
- **问题**：`re.search(r'"text"\s*:\s*"((?:[^"\\]|\\.)*)"', raw, re.S)` 报错 `unterminated character set at position 18`
- **根因**：`[^"\\]` 字符集语法错误，Python 正则解析器无法识别
- **解决**：简化为非贪婪匹配 `r'"text"\s*:\s*"(.*?)"'`

### 4. apt-get 安装卡住导致 workflow 超时
- **问题**：安装依赖步骤卡住 14+ 分钟，pip 安装成功但 apt-get 无响应
- **根因**：`apt-get update` 和 `pip install` 在同一 shell 步骤，apt 网络问题阻塞整个步骤
- **解决**：拆分成两个独立步骤（安装中文字体 + 安装依赖），方便定位问题

### 5. wordcloud 依赖 matplotlib
- **问题**：跳过 matplotlib 后词云生成报错 `No module named 'matplotlib'`
- **根因**：`wordcloud` 库内部 import 了 `matplotlib`（用于色彩映射）
- **解决**：不使用 `--no-deps`，让 pip 自动解析依赖（包含 matplotlib 全家桶）

### 6. pip 缓存与安装速度
- **优化**：`actions/setup-python@v5` 加 `cache: 'pip'`，缓存 PyPI 下载的 wheel 包
- **效果**：第一次运行约 2 分钟，之后 10 秒内完成依赖安装
- **注意**：apt 缓存因权限问题失败（`/var/cache/apt/archives` 无写权限），未使用
